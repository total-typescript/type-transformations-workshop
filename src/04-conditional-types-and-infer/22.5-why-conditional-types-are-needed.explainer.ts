// Daniel Rosenwasser explains why conditional types were needed in TS

/**
 * Daniel Rosenwasser is a developer of typescript and explained how thanks to Knockout.js, the conditional types actually got created.
 * Because they needed to know whether a type extend an array or an object in order to do something, otherwise just ignore it.
 */

export {};
